# Submissions

Each accepted module has one TOML file in this directory. Submit additions and updates through pull requests. Pull-request validation downloads every referenced release asset and verifies its manifest, source, size, and SHA-256 digest.

