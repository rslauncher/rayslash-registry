# Security policy

Report registry, signature, package-validation, impersonation, or malicious-module concerns through a private GitHub Security Advisory when disclosure could put users at risk. Use a public issue only for non-sensitive catalog errors.

The registry can delist a module and publish signed revocations. Delisting does not remotely delete user data.

